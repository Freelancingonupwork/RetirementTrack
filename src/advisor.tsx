import React, { useEffect, useMemo, useState } from "react";
import { Link, NavLink, Navigate, Route, Routes, useLocation } from "react-router-dom";
import { Activity, BellRing, Check, CheckCircle2, ChevronRight, CircleUserRound, ClipboardList, Clock3, Compass, Mail, Search, ShieldAlert, ShieldCheck, UsersRound } from "lucide-react";
import { initialState, type State } from "./domain";
import michaelCarterAvatar from "./assets/michael-carter-avatar.png";

type ReviewItem = { id:string; client:string; initials:string; type:"Planning Update"|"Assessment"|"Follow-up"; reason:string; date:string; priority:"High"|"Standard"; resolved:boolean };
type Engagement = { id:string; client:string; activity:string; type:"Planning"|"Assessment"|"Education"|"Checklist"|"Profile"; time:string };
type Outstanding = { id:string; client:string; activity:string; due:string; followUp:string; overdue?:boolean };
type Communication = { id:string; client:string; subject:string; status:"Scheduled"|"Sent"|"Escalated"; date:string };
type AdvisorDemo = { resolved:string[]; communications:Communication[] };
type FilterOption = { value:string; label:string };

const advisorKey = "retirementtrack-advisor-v1";
const learningTitles = ["What does your next chapter look like?", "Making the most of your planning check-in", "Putting investment risk into perspective", "A practical introduction to required distributions", "Your age 65 planning conversation", "Five retirement facts worth revisiting"];
const defaultAdvisor:AdvisorDemo = { resolved:[], communications:[
  { id:"message-1", client:"Sarah Smith", subject:"Planning Update reminder", status:"Scheduled", date:"September 23" },
  { id:"message-2", client:"Martin Cole", subject:"Assessment reminder", status:"Sent", date:"Yesterday" },
  { id:"message-3", client:"Ana Morales", subject:"Overdue Planning Update follow-up", status:"Escalated", date:"September 8" },
] };
const nav = [["dashboard","Advisor dashboard",Activity],["attention","Needs attention",ShieldAlert],["planning","Planning Updates",ClipboardList],["assessments","Assessments",ShieldCheck],["follow-ups","Follow-up / review",CheckCircle2],["communications","Communications",Mail],["activity","Client activity",Clock3],["clients","Client list",UsersRound]] as const;

function readClient():State { try { const saved=JSON.parse(localStorage.getItem("retirementtrack-demo-v1")||"null"); return saved?.version===1?saved:initialState(); } catch { return initialState(); } }
function clientExceptions(state:State):ReviewItem[] { const rows:ReviewItem[]=[]; if(state.planning.status==="Completed"&&state.planning.changes.length) rows.push({id:"sarah-planning",client:"Sarah Smith",initials:"SS",type:"Planning Update",reason:`${state.planning.changes.length} meaningful change${state.planning.changes.length>1?"s":""} detected`,date:"Today",priority:"High",resolved:false}); if(state.assessment.status==="Completed"&&state.exceptions.some(x=>x.sourceId===state.assessment.id)) rows.push({id:"sarah-assessment",client:"Sarah Smith",initials:"SS",type:"Assessment",reason:`Investment & Risk Assessment: ${state.assessment.result} meets the illustrative review rule`,date:"Today",priority:"Standard",resolved:false}); return rows; }
function staticExceptions():ReviewItem[] { return [
  {id:"elise-follow",client:"Elise Robinson",initials:"ER",type:"Follow-up",reason:"Retirement timeline changed from 67 to 65",date:"Yesterday",priority:"High",resolved:false},
  {id:"martin-assessment",client:"Martin Cole",initials:"MC",type:"Assessment",reason:"Client requested a conversation about the assessment result",date:"Sep 7",priority:"Standard",resolved:false},
  {id:"ana-planning",client:"Ana Morales",initials:"AM",type:"Planning Update",reason:"Planning Update passed the illustrative overdue threshold",date:"Sep 3",priority:"Standard",resolved:false},
]; }
function accomplishments(state:State):Engagement[] { const rows:Engagement[]=[]; if(state.profileComplete) rows.push({id:"sarah-profile",client:"Sarah Smith",activity:"Completed minimum profile",type:"Profile",time:"Today"}); if(state.planning.status==="Completed") rows.push({id:"sarah-update",client:"Sarah Smith",activity:state.planning.changes.length?"Completed Planning Update":"Completed Planning Update · no material changes",type:"Planning",time:"Today"}); if(state.assessment.status==="Completed") rows.push({id:"sarah-assessment-complete",client:"Sarah Smith",activity:`Completed Investment & Risk Assessment · ${state.assessment.result}`,type:"Assessment",time:"Today"}); state.read.forEach(id=>{const title=learningTitles[Number(id)];if(title)rows.push({id:`sarah-learning-${id}`,client:"Sarah Smith",activity:`Completed “${title}”`,type:"Education",time:"Today"})}); return [...rows,{id:"elise-checklist",client:"Elise Robinson",activity:"Completed Retirement Preparedness Checklist",type:"Checklist",time:"Yesterday"},{id:"martin-rmd",client:"Martin Cole",activity:"Explored RMD education",type:"Education",time:"Sep 7"}]; }
function buildOutstanding(state:State):Outstanding[] { const rows:Outstanding[]=[]; if(state.planning.status==="Due") rows.push({id:"sarah-update-due",client:"Sarah Smith",activity:"Quarterly Planning Update",due:"Due September 30",followUp:"Reminder scheduled September 23"}); if(state.assessment.status!=="Completed") rows.push({id:"sarah-assessment-open",client:"Sarah Smith",activity:"Investment & Risk Assessment",due:"Recommended",followUp:"RetirementTrack will follow up"}); return [...rows,{id:"elise-checklist-open",client:"Elise Robinson",activity:"Medicare readiness checklist",due:"Due in 12 days",followUp:"Reminder scheduled"},{id:"ana-update-overdue",client:"Ana Morales",activity:"Planning Update",due:"Overdue 7 days",followUp:"Escalated after reminder sequence",overdue:true}]; }
function useAdvisorState(){ const[demo,setDemo]=useState<AdvisorDemo>(()=>{try{const saved=JSON.parse(localStorage.getItem(advisorKey)||"null");return{...defaultAdvisor,resolved:Array.isArray(saved?.resolved)?saved.resolved:[]}}catch{return defaultAdvisor}}); const[client,setClient]=useState<State>(readClient); useEffect(()=>localStorage.setItem(advisorKey,JSON.stringify(demo)),[demo]); const refresh=()=>setClient(readClient()); const items=useMemo(()=>[...clientExceptions(client),...staticExceptions()].map(x=>({...x,resolved:demo.resolved.includes(x.id)})),[client,demo.resolved]); const engagement=useMemo(()=>accomplishments(client),[client]); const outstanding=useMemo(()=>buildOutstanding(client),[client]); return{demo,setDemo,client,items,engagement,outstanding,refresh}; }
type Workspace=ReturnType<typeof useAdvisorState>;

function includesQuery(query:string,...values:Array<string|undefined>){const q=query.trim().toLowerCase();return !q||values.some(value=>value?.toLowerCase().includes(q));}
function AdvisorHeader({title}:{eyebrow:string;title:string;children?:React.ReactNode}){return title.startsWith("Good morning,")?<header className="advisor-heading dashboard-greeting"><h1 tabIndex={-1}>{title}</h1></header>:null}
function AdvisorTools({query,setQuery,placeholder,resultLabel,filter,setFilter,filterLabel="Filter results",options}:{query:string;setQuery:(value:string)=>void;placeholder:string;resultLabel:string;filter?:string;setFilter?:(value:string)=>void;filterLabel?:string;options?:FilterOption[]}){return <section className="advisor-tools" aria-label="Page search and filters"><label className="advisor-search"><Search size={17}/><span className="sr-only">Search</span><input value={query} onChange={event=>setQuery(event.target.value)} placeholder={placeholder}/></label>{options&&filter!==undefined&&setFilter&&<label className="advisor-select"><span>{filterLabel}</span><select value={filter} onChange={event=>setFilter(event.target.value)}>{options.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select></label>}<span className="advisor-result-count" aria-live="polite">{resultLabel}</span></section>}
function EmptyResults({title="No matching results."}:{title?:string}){return <div className="advisor-empty compact"><Search/><h2>{title}</h2><p>Try a different client name, activity, or status.</p></div>}

export function AdvisorWorkspace(){const state=useAdvisorState();const location=useLocation();useEffect(()=>{state.refresh();document.querySelector<HTMLHeadingElement>("h1")?.focus();window.scrollTo(0,0)},[location.pathname]);const label=nav.find(([path])=>location.pathname.includes(path))?.[1]||"Advisor dashboard";const count=state.items.filter(x=>!x.resolved).length;return <div className="advisor-app"><a className="skip" href="#advisor-main">Skip to advisor content</a><aside className="advisor-sidebar"><Link to="/advisor/dashboard" className="advisor-brand"><Compass size={30}/><span>Retirement<span className="brand-light">Track</span><small>BY HARBOR WEALTH</small></span></Link><nav aria-label="Advisor navigation">{nav.map(([path,title,Icon])=><NavLink key={path} to={`/advisor/${path}`}><Icon size={18}/><span>{title}</span>{path==="attention"&&count>0&&<i>{count}</i>}</NavLink>)}</nav><div className="advisor-sidebar-foot"><div className="advisor-sidebar-profile"><img src={michaelCarterAvatar} alt="Michael Carter"/><span><strong>Michael Carter</strong><small>Advisor · 84 clients</small></span></div><Link to="/client/dashboard">View client portal <ChevronRight size={14}/></Link></div></aside><section className="advisor-workspace"><div className="advisor-topbar"><span>Advisor workspace <ChevronRight size={14}/> <strong>{label}</strong></span><div><span className="demo-pill">DISCOVERY PROTOTYPE</span><button aria-label="Refresh local client activity" className="advisor-refresh" onClick={state.refresh}>Refresh</button><span className="advisor-avatar">MC</span></div></div><main id="advisor-main" className="advisor-main"><Routes><Route path="dashboard" element={<Dashboard {...state}/>}/><Route path="attention" element={<Attention {...state}/>}/><Route path="planning" element={<PlanningQueue {...state}/>}/><Route path="assessments" element={<AssessmentQueue {...state}/>}/><Route path="follow-ups" element={<FollowUps {...state}/>}/><Route path="communications" element={<Communications {...state}/>}/><Route path="activity" element={<RecentActivity {...state}/>}/><Route path="clients" element={<ClientList {...state}/>}/><Route path="*" element={<Navigate to="dashboard" replace/>}/></Routes></main></section></div>}
function resolveItem(setDemo:React.Dispatch<React.SetStateAction<AdvisorDemo>>,id:string){setDemo(state=>({...state,resolved:state.resolved.includes(id)?state.resolved:[...state.resolved,id]}))}
function ReviewCard({item,resolve}:{item:ReviewItem;resolve:(id:string)=>void}){return <article className={`advisor-review ${item.resolved?"is-resolved":""}`}><div className="advisor-initials">{item.initials}</div><div className="advisor-review-main"><div><span className={`priority priority-${item.priority.toLowerCase()}`}>{item.priority}</span><span className="review-type">Exception · {item.type}</span></div><h3>{item.client}</h3><p>{item.reason}</p><small>{item.date}</small></div><div className="advisor-review-action">{item.resolved?<span><Check size={15}/> Resolved</span>:<button className="button secondary" onClick={()=>resolve(item.id)}>Review exception <ChevronRight size={15}/></button>}</div></article>}
function ActivityRows({events,limit}:{events:Engagement[];limit?:number}){if(!events.length)return <EmptyResults/>;return <section className="activity-rows">{events.slice(0,limit).map(item=><div key={item.id}><Activity size={17}/><div><strong>{item.client} · {item.activity}</strong><small>{item.type} · No action needed</small></div><span>{item.time}</span></div>)}</section>}
function OutstandingRows({items,limit}:{items:Outstanding[];limit?:number}){if(!items.length)return <EmptyResults/>;return <section className="advisor-outstanding-list">{items.slice(0,limit).map(item=><article key={item.id} className={item.overdue?"is-overdue":""}><Clock3 size={18}/><div><strong>{item.client} · {item.activity}</strong><span>{item.due}</span><small>{item.followUp}</small></div><em>{item.overdue?"Exception":"Platform follow-up"}</em></article>)}</section>}

function Dashboard({items,setDemo,engagement,outstanding,refresh}:Workspace){const[query,setQuery]=useState("");const open=items.filter(item=>!item.resolved);const visibleEngagement=engagement.filter(item=>includesQuery(query,item.client,item.activity,item.type));const visibleExceptions=open.filter(item=>includesQuery(query,item.client,item.reason,item.type,item.priority));const resultCount=visibleEngagement.length+visibleExceptions.length;return <><AdvisorHeader eyebrow="CLIENT VISIBILITY · EXCEPTION-BASED SUPPORT" title="Good morning, Michael.">See client progress, outstanding activities, and the exceptions where your guidance may help.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search clients, activities, or exceptions" resultLabel={`${resultCount} matching records`}/><section className="advisor-summary advisor-summary-philosophy"><div><span>Client accomplishments</span><strong>{engagement.length}</strong><small>Recent learning and actions</small></div><div><span>Outstanding</span><strong>{outstanding.length}</strong><small>Platform-managed follow-up</small></div><div><span>Needs attention</span><strong>{open.length}</strong><small>Advisor exceptions</small></div><div><span>Assigned clients</span><strong>84</strong><small>Visible client journeys</small></div></section><section className="advisor-operating-note"><BellRing size={20}/><div><strong>RetirementTrack handles routine guidance and reminders.</strong><span>You maintain visibility and step in when a client asks or an approved exception rule is met.</span></div></section><div className="advisor-layout"><section><div className="advisor-section-title"><div><p>CLIENT ENGAGEMENT</p><h2>Recent accomplishments</h2></div><Link to="/advisor/activity">View all activity <ChevronRight size={15}/></Link></div><ActivityRows events={visibleEngagement} limit={4}/><div className="advisor-section-title split"><div><p>EXCEPTION-BASED SUPPORT</p><h2>Needs advisor attention</h2></div><Link to="/advisor/attention">View all <ChevronRight size={15}/></Link></div><div className="advisor-card-grid">{visibleExceptions.slice(0,4).map(item=><ReviewCard key={item.id} item={item} resolve={id=>resolveItem(setDemo,id)}/>)}{!visibleExceptions.length&&<EmptyResults/>}</div></section><aside className="advisor-rail"><section className="advisor-rail-card"><p>IMPORTANT OUTSTANDING</p><h2>{outstanding.length} client activities</h2><span>Routine follow-up remains with RetirementTrack until an exception threshold is reached.</span><Link to="/advisor/planning">Review outstanding <ChevronRight size={15}/></Link></section><section className="advisor-rail-card"><p>AUTOMATED FOLLOW-UP</p><h2>2 reminders active</h2><span>One reminder is scheduled and one was recently sent. No advisor action is required.</span><Link to="/advisor/communications">View reminder activity <ChevronRight size={15}/></Link></section><section className="advisor-rail-card advisor-client-count"><UsersRound size={22}/><h2>84 assigned clients</h2><span>See accomplishments, upcoming work, and exceptions in each client journey.</span><Link to="/advisor/clients">Open client list <ChevronRight size={15}/></Link></section></aside></div><button className="advisor-sync" onClick={refresh}>Refresh local client-demo activity</button></>}

function Attention({items,setDemo}:Workspace){const[query,setQuery]=useState("");const[priority,setPriority]=useState("all");const open=items.filter(item=>!item.resolved&&(priority==="all"||item.priority===priority)&&includesQuery(query,item.client,item.reason,item.type));return <><AdvisorHeader eyebrow="NEEDS ATTENTION" title="Exceptions, with a clear reason.">Only client requests, meaningful changes, approved review conditions, and sufficiently overdue activity appear here.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search exceptions or clients" resultLabel={`${open.length} open exceptions`} filter={priority} setFilter={setPriority} filterLabel="Priority" options={[{value:"all",label:"All priorities"},{value:"High",label:"High priority"},{value:"Standard",label:"Standard priority"}]}/><div className="advisor-section-actions"><span>Advisor action is limited to true exceptions.</span><button className="button secondary" onClick={()=>setDemo(state=>({...state,resolved:[]}))}>Restore demo queue</button></div><div className="advisor-card-grid">{open.map(item=><ReviewCard key={item.id} item={item} resolve={id=>resolveItem(setDemo,id)}/>)}{!open.length&&<div className="advisor-empty"><CheckCircle2/><h2>You are up to date.</h2><p>No matching client exceptions require attention.</p></div>}</div></>}

function PlanningQueue({items,setDemo,client,outstanding}:Workspace){const[query,setQuery]=useState("");const[view,setView]=useState("all");const exceptions=items.filter(item=>item.type==="Planning Update"&&!item.resolved&&view!=="routine"&&includesQuery(query,item.client,item.reason,item.type));const routine=outstanding.filter(item=>item.activity.includes("Planning Update")&&view!=="exceptions"&&includesQuery(query,item.client,item.activity,item.due,item.followUp));return <><AdvisorHeader eyebrow="RECURRING CLIENT UPDATES" title="Planning Updates">See routine status separately from updates that require professional review.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search Planning Updates or clients" resultLabel={`${routine.length+exceptions.length} matching updates`} filter={view} setFilter={setView} filterLabel="Queue" options={[{value:"all",label:"All updates"},{value:"routine",label:"Platform-managed"},{value:"exceptions",label:"Advisor exceptions"}]}/>{view!=="exceptions"&&<section className="advisor-page-section"><div className="advisor-section-title"><div><p>PLATFORM-MANAGED</p><h2>Outstanding updates</h2></div></div><OutstandingRows items={routine}/></section>}{view!=="routine"&&<section className="advisor-page-section"><div className="advisor-section-title"><div><p>ADVISOR ATTENTION</p><h2>Meaningful changes and overdue exceptions</h2></div></div><section className="advisor-context"><ClipboardList/><span>{client.planning.status==="Completed"?(client.planning.changes.length?`Sarah submitted an update with ${client.planning.changes.length} meaningful change(s).`:"Sarah submitted an update with no material changes; no advisor task was created."):"Sarah’s update is due September 30. RetirementTrack will send the routine reminder."}</span></section><div className="advisor-card-grid">{exceptions.map(item=><ReviewCard key={item.id} item={item} resolve={id=>resolveItem(setDemo,id)}/>)}{!exceptions.length&&<EmptyResults title="No matching Planning Update exceptions."/>}</div></section>}</>}

function AssessmentQueue({items,setDemo,client,outstanding}:Workspace){const[query,setQuery]=useState("");const[view,setView]=useState("all");const exceptions=items.filter(item=>item.type==="Assessment"&&!item.resolved&&view!=="routine"&&includesQuery(query,item.client,item.reason,item.type));const routine=outstanding.filter(item=>item.activity.includes("Assessment")&&view!=="exceptions"&&includesQuery(query,item.client,item.activity,item.due));return <><AdvisorHeader eyebrow="ASSESSMENT VISIBILITY" title="Assessments">Completion is visible; advisor work appears only when an approved rule or client request applies.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search assessments or clients" resultLabel={`${routine.length+exceptions.length} matching assessments`} filter={view} setFilter={setView} filterLabel="Status" options={[{value:"all",label:"All assessments"},{value:"routine",label:"Platform-managed"},{value:"exceptions",label:"Advisor exceptions"}]}/>{view!=="exceptions"&&<section className="advisor-page-section"><div className="advisor-section-title"><div><p>CLIENT PROGRESS</p><h2>Available and incomplete</h2></div></div><OutstandingRows items={routine}/></section>}{view!=="routine"&&<section className="advisor-page-section"><div className="advisor-section-title"><div><p>ADVISOR ATTENTION</p><h2>Review cues</h2></div></div><section className="advisor-context"><ShieldCheck/><span>{client.assessment.status==="Completed"?`Sarah completed the assessment with a ${client.assessment.result} result.`:"Sarah’s assessment remains available. RetirementTrack will provide routine follow-up."}</span></section><div className="advisor-card-grid">{exceptions.map(item=><ReviewCard key={item.id} item={item} resolve={id=>resolveItem(setDemo,id)}/>)}{!exceptions.length&&<EmptyResults title="No matching assessment exceptions."/>}</div></section>}</>}

function FollowUps({items,setDemo}:Workspace){const[query,setQuery]=useState("");const[priority,setPriority]=useState("all");const rows=items.filter(item=>item.type==="Follow-up"&&!item.resolved&&(priority==="all"||item.priority===priority)&&includesQuery(query,item.client,item.reason));return <><AdvisorHeader eyebrow="ACTIVE CONVERSATIONS" title="Follow-up / review">Focused next steps initiated by a client or created from an approved exception.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search follow-ups or clients" resultLabel={`${rows.length} active follow-ups`} filter={priority} setFilter={setPriority} filterLabel="Priority" options={[{value:"all",label:"All priorities"},{value:"High",label:"High priority"},{value:"Standard",label:"Standard priority"}]}/><section className="advisor-context"><CircleUserRound/><span>Client-requested contact and exception-based reviews stay visible here. Routine reminders remain automated.</span></section><div className="advisor-card-grid">{rows.map(item=><ReviewCard key={item.id} item={item} resolve={id=>resolveItem(setDemo,id)}/>)}{!rows.length&&<EmptyResults title="No matching active follow-ups."/>}</div></>}

function Communications({demo}:Workspace){const[query,setQuery]=useState("");const[status,setStatus]=useState("all");const rows=demo.communications.filter(item=>(status==="all"||item.status===status)&&includesQuery(query,item.client,item.subject,item.status,item.date));return <><AdvisorHeader eyebrow="AUTOMATED COMMUNICATION VISIBILITY" title="Reminder activity">RetirementTrack handles routine reminders. You retain visibility without routine approval work.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search reminders or clients" resultLabel={`${rows.length} communication records`} filter={status} setFilter={setStatus} filterLabel="Delivery status" options={[{value:"all",label:"All statuses"},{value:"Scheduled",label:"Scheduled"},{value:"Sent",label:"Sent"},{value:"Escalated",label:"Escalated"}]}/><section className="advisor-communication-grid">{rows.map(item=><article className="advisor-review advisor-communication" key={item.id}><div className="advisor-initials"><BellRing size={17}/></div><div className="advisor-review-main"><span className={`communication-status status-${item.status.toLowerCase()}`}>{item.status}</span><h3>{item.client}</h3><p>{item.subject}</p><small>{item.date}</small></div><div className="advisor-review-action"><span>{item.status==="Escalated"?<><ShieldAlert size={15}/> Advisor visibility</>:<><Check size={15}/> No action needed</>}</span></div></article>)}{!rows.length&&<EmptyResults/>}</section><section className="advisor-context"><Mail/><span>Delivery channels, consent rules, templates, and production scheduling remain future implementation decisions.</span></section></>}

function RecentActivity({engagement}:Workspace){const[query,setQuery]=useState("");const[type,setType]=useState("all");const rows=engagement.filter(item=>(type==="all"||item.type===type)&&includesQuery(query,item.client,item.activity,item.type,item.time));return <><AdvisorHeader eyebrow="CLIENT ENGAGEMENT" title="Client accomplishments">See what clients have completed, reviewed, or explored. These records provide context without automatically creating work.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search client activity" resultLabel={`${rows.length} accomplishment records`} filter={type} setFilter={setType} filterLabel="Activity type" options={[{value:"all",label:"All activity"},{value:"Planning",label:"Planning Updates"},{value:"Assessment",label:"Assessments"},{value:"Education",label:"Education"},{value:"Checklist",label:"Checklists"},{value:"Profile",label:"Profiles"}]}/><ActivityRows events={rows}/></>}

function ClientList({items,client,engagement,outstanding}:Workspace){const[selected,setSelected]=useState("Sarah Smith");const[query,setQuery]=useState("");const[status,setStatus]=useState("all");const clients=[{name:"Sarah Smith",stage:"Approaching retirement",status:items.some(item=>item.client==="Sarah Smith"&&!item.resolved)?"Needs attention":"Current",last:"Today"},{name:"Elise Robinson",stage:"Approaching retirement",status:"Current",last:"Yesterday"},{name:"Martin Cole",stage:"Building toward retirement",status:"Client requested contact",last:"Sep 7"},{name:"Ana Morales",stage:"Already retired",status:"Overdue exception",last:"Sep 3"}];const visible=clients.filter(item=>(status==="all"||(status==="attention"?item.status!=="Current":item.status==="Current"))&&includesQuery(query,item.name,item.stage,item.status,item.last));const current=clients.find(item=>item.name===selected)||clients[0];const isSarah=selected==="Sarah Smith";const done=engagement.filter(item=>item.client===selected).length;const remaining=outstanding.filter(item=>item.client===selected).length;const exceptions=items.filter(item=>item.client===selected&&!item.resolved).length;return <><AdvisorHeader eyebrow="ASSIGNED CLIENTS" title="Client journeys">Select a client to see progress, outstanding activity, and exceptions together.</AdvisorHeader><AdvisorTools query={query} setQuery={setQuery} placeholder="Search clients, life stage, or status" resultLabel={`${visible.length} matching clients`} filter={status} setFilter={setStatus} filterLabel="Client status" options={[{value:"all",label:"All clients"},{value:"current",label:"Current"},{value:"attention",label:"Needs attention"}]}/><div className="advisor-client-layout"><section className="advisor-client-table"><div className="advisor-client-head"><span>Client</span><span>Life stage</span><span>Current state</span><span>Last activity</span></div>{visible.map(item=><button type="button" className={`advisor-client-row ${selected===item.name?"selected":""}`} key={item.name} onClick={()=>setSelected(item.name)}><span className="client-name"><span className="advisor-initials small">{item.name.split(" ").map(part=>part[0]).join("")}</span><strong>{item.name}</strong></span><span>{item.stage}</span><span>{item.status}</span><span>{item.last}</span></button>)}{!visible.length&&<div className="advisor-table-empty">No clients match this search.</div>}</section><aside className="advisor-client-journey"><p>CLIENT JOURNEY</p><h2>{current.name}</h2><span>{current.stage}</span><div className="journey-metric"><strong>{isSarah?done:2}</strong><span>Recent accomplishments</span></div><div className="journey-metric"><strong>{remaining}</strong><span>Outstanding activities</span></div><div className="journey-metric"><strong>{exceptions}</strong><span>Open exceptions</span></div>{isSarah&&<div className="journey-detail"><small>LATEST CONTEXT</small><span>Planning Update: {client.planning.status}</span><span>Assessment: {client.assessment.status}</span><span>Learning resources completed: {client.read.length}</span></div>}<Link to="/advisor/activity">View engagement history <ChevronRight size={15}/></Link></aside></div></>}
